import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.mentriq360.erp",
  appName: "Mentriq360 ERP",
  webDir: "out",
  server: {
    androidScheme: "https"
  }
};

export default config;
